# SVG icon rule

When an interface needs a conventional icon, follow `svg-icon-agent`. Prefer the existing SVG/icon system, then raw inline SVG or a local `.svg` asset. Do not use PNG/JPG/WebP raster icons or emoji when an SVG equivalent is available.

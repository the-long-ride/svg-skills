# SVG icon rule

Use `.agents/skills/svg-icon-agent/SKILL.md` for interface icon work. Prefer existing SVG components, then raw SVG markup or local `.svg` assets. Do not use PNG/JPG/WebP or emoji as fallback UI iconography when SVG is available.

# SVG icon rule

Codex discovers `svg-icon-agent` from `~/.agents/skills/svg-icon-agent` and reads `agents/openai.yaml`. For UI icons, prefer existing SVG components, then raw SVG markup or local `.svg` assets. Do not use PNG/JPG/WebP raster icons or emoji when an SVG equivalent is available.

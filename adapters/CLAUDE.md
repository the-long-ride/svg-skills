# SVG icon rule

When implementing UI, load `svg-icon-agent`. Prefer the existing SVG system, then raw inline SVG or local `.svg` assets. Do not substitute PNG/JPG/WebP raster icons or emoji for conventional UI icons when SVG is available.

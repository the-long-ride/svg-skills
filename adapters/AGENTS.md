# SVG icon rule

For UI icons, use the `svg-icon-agent` skill. Prefer the existing SVG/icon system, then raw inline SVG or a local `.svg` asset. Do not introduce PNG/JPG/WebP raster icons or emoji when a conventional SVG icon is appropriate. Preserve source/license sidecars for SVG Repo assets.
